HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Comma-separated list of allowed CORS origins (exact match)
# Example: http://localhost:3000,https://your-app.lovable.app
CORS_ORIGINS=http://localhost:3000

# Comma-separated wildcard patterns for CORS origins (use * to match a single subdomain segment)
# Useful for dynamic preview URLs that change on each deploy (e.g. Lovable editor previews)
# Example: https://*.lovable.app
CORS_ORIGIN_PATTERNS=
